server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # THIS IS THE MAGIC LINE: It sends all requests back to index.html
        try_files $uri $uri/ /index.html; 
    }
}